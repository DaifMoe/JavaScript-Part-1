// JavaScript function has a name function name and parameter function name(parameter)
// To call function write it's name where it needs to be called and pass it arguments

function greet(name, lastName) {
  console.log("Hello " + name + " " + lastName);
}

greet("John", "Mikeson");
greet("Mike", "Johnson");
