let namez = "Moe"; // This is a string literal (string)
let age = 20; // This is a number literal (number)
let isApproved = true; // This is a boolean literal (boolean)
let firstName = undefined;
let selectedColor = null; // We use this when we want to explicitly clear the value of a variable
