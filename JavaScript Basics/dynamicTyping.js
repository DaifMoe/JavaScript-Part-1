// JavaScript is a Dynamically Typed Language meaning that
// if we assign a value to a variable it can be reassigned later

// Java is a static language so it expects variables to be declared
// before they can be assigned values
let namez = "Moe";
let age = 20;
let isApproved = true;
let firstName = undefined;
let selectedColor = null;
